# Upgrade respo-calcit-workflow to Calcit 0.13.77

- Raise the template compiler baseline from 0.13.64 to 0.13.77 and pin the JavaScript runtime package to the exact matching version.
- Pin setup-calcit 1.4.0 to its immutable commit and update the published-action output assertion to 0.13.77 while preserving the independently pinned Caps 0.1.0 check.
- Preserve package version 0.0.4, template behavior, strict module/toolchain validation, static quality baseline, Markdown checks, JS/Vite build, and push-only deployment.
- Align the direct Respo family dependencies with their latest published graph: Respo 0.16.90, Reel 0.6.12, and Respo UI 0.7.15.
- Correct two project-local 0.13.77 boundaries through the Calcit CLI: update the `Store.states` field instead of passing the nominal Store to a Map API, and validate `document.visibilityState` with `js-ffi.contract/expect-string` before comparison.

## Blocked validation

- Strict Caps, immutable Yarn, exact Calcit/procs 0.13.77 verification, all five published module links, and canonical formatting pass.
- Published Respo 0.16.90 leaves exactly one check-only warning at `respo.core/decorate-defcomp`; the merged fix is waiting for a consumable release in calcit #670.
- A temporary source-link to the clean post-fix Respo worktree proves both entries pass check-only; check-types, weak-types, deprecated, dynamic-methods zero, the unchanged quality baseline, README formatting/checking, JS generation, and the Node 24.4.1/Vite 8.0.12 production build all pass.
- The project-local module link was restored to immutable published Respo 0.16.90 after diagnosis. No pull request should be opened before the fixed Respo release is consumable and the full workflow passes without a source link.

## 中文

- 将模板编译器基线从 0.13.64 升级到 0.13.77，并把 JavaScript runtime package 精确固定到相同版本。
- 将 setup-calcit 1.4.0 固定到不可变 commit，把发布 action 的输出断言更新为 0.13.77，同时保留独立固定的 Caps 0.1.0 检查。
- 保持包版本 0.0.4、模板行为、strict 模块/工具链验证、静态质量 baseline、Markdown 检查、JS/Vite 构建及仅 main push 部署不变。
- 将 Respo 系列直依赖同步到最新已发布图：Respo 0.16.90、Reel 0.6.12 与 Respo UI 0.7.15。
- 仅通过 Calcit CLI 修正两处项目自身的 0.13.77 边界：更新 `Store.states` 字段而非把 nominal Store 传给 Map API，并在比较前用 `js-ffi.contract/expect-string` 验证 `document.visibilityState`。

## 验证阻塞

- strict Caps、immutable Yarn、精确 Calcit/procs 0.13.77、全部 5 个已发布 module link 及规范格式均通过。
- 已发布 Respo 0.16.90 仅剩 `respo.core/decorate-defcomp` 一条 check-only warning；对应合并修复仍在 calcit #670 等待可消费版本。
- 临时 source-link 到干净的 Respo 修复后 worktree 可证明：两个 entry 的 check-only、check-types、weak-types、deprecated、dynamic-methods 零容忍、未变更 quality baseline、README 格式/代码检查、JS 生成，以及 Node 24.4.1/Vite 8.0.12 production build全部通过。
- 诊断后已将 project-local module link 恢复到不可变的已发布 Respo 0.16.90。修复版可消费且完整 workflow 无需 source link 通过前，不应创建 PR。
